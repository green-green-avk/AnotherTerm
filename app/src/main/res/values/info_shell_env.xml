<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <string name="desc_shell_env_help" tools:ignore="TypographyDashes,TypographyEllipsis"><![CDATA[
        <p>Environment variables:
        <ul>
        <li><clipboard>DATA_DIR</clipboard> &#x2014; default persistent data directory.
        <br/><b>It will be deleted when this application is uninstalled.</b></li>
        <li><clipboard>PROTECTED_DATA_DIR</clipboard> &#x2014; default device-protected persistent
        data directory (set if available).
        <br/><b>It will be deleted when this application is uninstalled.</b></li>
        <li><clipboard>SHARED_DATA_DIR</clipboard> &#x2014;
        directory on the primary shared/external storage device
        where the application can place persistent files it owns
        (set if available).
        Any other application with the storage access permissions can also access this place.
        <br/><b>It will be deleted when this application is uninstalled.</b></li>
        <li><clipboard>EXTERNAL_DATA_DIR</clipboard> &#x2014;
        an alias to <clipboard>SHARED_DATA_DIR</clipboard>.</li>
        <li><clipboard>PUBLIC_DATA_DIR</clipboard> &#x2014; the root of the \"sdcard\".
        <code>READ_EXTERNAL_STORAGE</code> / <code>WRITE_EXTERNAL_STORAGE</code> permissions are
        required.</li>
        <li><clipboard>LIB_DIR</clipboard> &#x2014; native binaries path.</li>
        <li><clipboard>APP_APK</clipboard> &#x2014; path to the <b>Another Term</b> APK.</li>
        <li><clipboard>APP_ID</clipboard> &#x2014; <b>Another Term</b> application id.</li>
        <li><clipboard>APP_VERSION</clipboard> &#x2014; <b>Another Term</b> version.</li>
        <li><clipboard>APP_TARGET_SDK</clipboard> &#x2014; <b>Another Term</b>
        <code>targetSdkVersion</code>.</li>
        <li><clipboard>MY_DEVICE_ABIS</clipboard> &#x2014; supported architectures list
        from the most preferable to the least preferable.</li>
        <li><clipboard>MY_ANDROID_SDK</clipboard> &#x2014; Android SDK version.</li>
        <li><clipboard>SHELL_SESSION_TOKEN</clipboard> &#x2014; associated with related data
        including <b>termsh</b> permissions and the session related UI.</li>
        </ul>
        See also:
        <ul>
        <li><a href=\"info:/share_input\">Content sharing with Another Term</a></li>
        </ul>
        </p>
        <p>Commands:
        <ul>
        <li><b>termsh</b> (<clipboard>$LIB_DIR/libtermsh.so</clipboard>) &#x2014; a tool to
        use Android functionality from the shell.
        Run with <code>help</code> command for
        <a href=\"info:/termsh_man\">details</a>.</li>
        </ul></p>
    ]]></string>
</resources>
